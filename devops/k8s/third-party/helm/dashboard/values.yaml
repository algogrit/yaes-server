# serviceAccount:
#   create: true
#   name: admin-user
# protocolHttp: true
ingress:
  enabled: true
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
  hosts:
    - dashboard.yaes-local.com
